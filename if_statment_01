#include "main.h"

/**
 * handel_if - handle if statment
 * @tokens_line: the line of tokens
 * 
 * Return: void
*/

void handel_if(char **tokens_line)
{
    int i;

    for (i = 0; tokens_line[i] != NULL; i++)
    {
        printf("tokens_line[%d]: %s\n", i, tokens_line[i]);
    }
}